#ifndef SPECIL_STRING_H
#define SPECIL_STRING_H
#include <iostream>
#include "string.h"
#define  MAX_PATH 100
char* RemoveSpcSymbol(char* str);

void reverse(char *left, char *right);
#endif
